import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.deezelll.idlefishingvillage',
  appName: 'Idle Fishing Village',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;

